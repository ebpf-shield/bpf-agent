{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "go: generate & build firewall (debug)",
            "type": "shell",
            "command": "bash",
            "args": [
                "-lc",
                "go generate && go build -gcflags=all='-N -l' -o ${workspaceFolder}/debug_firewall ."
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ]
}